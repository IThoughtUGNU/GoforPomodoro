name: GoforPomodoro
on: 
  push:
    branches:
      - 'main'
jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Build & Deploy
        run: |
          cd ${{ github.workspace }}
          docker-compose up -d --build
